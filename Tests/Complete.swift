import Foundation

final class CompleteLocalizationManager {
    let basicKey = "basic.key"
    let basicUnderscoreKey = "basic_key"
    let basicCamelKey = "basicKey"
    let multilineKey = "multiline.key"
    let escapedQuotes = "escaped.quotes"
    let escapedCharacters = "escaped.key"
    let unicodeKey = "unicode.key"
    let unicodeEmoji = "unicode.emoji"
    let unicodeText = "unicode.text"
    let noSpaces = "no.spaces"
    let manySpaces = "many.spaces"
    let tabSpaces = "tab.spaces"
    let spacesKey = "spaces.key"
    let afterComment = "after.comment"
    let afterMultilineComment = "after.multiline.comment"
    let inlineComment = "inline.comment"
    let commentKey = "comment.key"
    let keyWithDots = "key.with.dots"
    let keyWithUnderscore = "key_with_underscore"
    let keyWithDash = "key-with-dash"
    let keyWithSpaces = "key with spaces"
    let keyWithSlashes = "key/with/slashes"
    let keyWithPlus = "key+with+plus"
    let keyWithAmpersand = "key&with&ampersand"
    let keyWithHash = "key#with#hash"
    let keyWithAt = "key@with@at"
    let semicolonInValue = "semicolon.in.value"
    let multipleSemicolons = "multiple.semicolons"
    let emptyValue = "empty.value"
    let emptyValueWithSpaces = "empty.value.with.spaces"
    let complexValue = "complex.value"
    let trailingSpaces = "trailing.spaces"
    let doubleSemicolon = "double.semicolon"
    let missingSemicolon = "missing.semicolon"
    let missingValue = "missing.value"
    let missingEqualsSign = "missing.equals.sign"
    let missingQuotes = "missing.quotes"
    let htmlValue = "html.value"
    let jsonValue = "json.value"
    let urlValue = "url.value"
    let regexValue = "regex.value"
    let mathValue = "math.value"
    let formulaValue = "formula.value"
    let chineseSimplifiedKey = "chinese.simplified.key"
    let chineseTraditionalKey = "chinese.traditional.key"
    let japaneseKey = "japanese.key"
    let koreanKey = "korean.key"
    let arabicKey = "arabic.key"
    let russianKey = "russian.key"
    let hindiKey = "hindi.key"
    let thaiKey = "thai.key"
    let emojiValue = "emoji.value"
    let placeholderValue = String(format: "placeholder.value", "John")
    let formatValue = String(format: "format.value", 42, "John", 3.14)
    let base64Value = "base64.value"
    let mixedLanguagesKey = "mixed.languages.key"
}
